# Core ML dependencies
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# API dependencies  
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Note: Using flexible version ranges for Python 3.13 compatibility
# If pydantic fails to install, try: pip install --upgrade pip wheel setuptools
